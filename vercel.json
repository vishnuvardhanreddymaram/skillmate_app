{
  "buildCommand": "if [ -d \"flutter\" ]; then cd flutter && git pull && cd .. ; else git clone --depth 1 -b stable https://github.com/flutter/flutter.git; fi && export PATH=\"$PATH:$(pwd)/flutter/bin\" && flutter config --enable-web && flutter pub get && flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
